name: Verificador de Scripts
on: [push]
jobs:
  testes:
    runs-on: unbutu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Rodar ShellCheck
        uses: ludeeus/action-shellcheck@master
